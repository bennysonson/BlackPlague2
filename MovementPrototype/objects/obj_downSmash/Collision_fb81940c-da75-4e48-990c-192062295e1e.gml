/// @description Insert description here
// You can write your code in this editor
instance_destroy();
with (instance_create_layer(x-200,y-200,layer,obj_regularText)) {
		text = other.text;
		length = string_length(text);
}